type Skill {
  id: ID
  name: String!
  desc: String
  rating: Int
  fa: String
  color: String
}

type Info {
  id: ID
  type: String!
  info: String
  link: String
}

type Query {
  skills: [Skill]
  contact: [Info]
}
