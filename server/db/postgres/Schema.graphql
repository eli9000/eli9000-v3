type Skill {
    id: ID
    name: String!
    desc: String
    rating: Int
}

type Info {
    id: ID
    type: String!
    info: String
    link: String
}

type Query {
    skills: [Skill]
    contact: [Info]
}
